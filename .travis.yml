before_install:
  - chmod +x KT-34751/gradlew
  - chmod +x kodein/gradlew
script:
  - cd KT-34751
  - ./gradlew clean build
  - cd ..
  - cd kodein
  - ./gradlew clean build
  - cd ..
notifications:
  email: false